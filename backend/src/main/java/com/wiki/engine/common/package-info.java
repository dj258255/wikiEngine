/**
 * 공통 모듈.
 * ErrorCode, BusinessException, GlobalExceptionHandler 등
 * 모든 모듈에서 공유하는 횡단 관심사를 포함한다.
 */
@org.springframework.modulith.ApplicationModule(
        allowedDependencies = {}
)
package com.wiki.engine.common;
